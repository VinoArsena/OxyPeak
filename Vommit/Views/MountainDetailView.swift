import SwiftUI

struct MountainDetailView: View {
    
    var body: some View {
        Text("Text")
    }
}

#Preview {
    MountainDetailView()
        .preferredColorScheme(.dark)
        .background(Color("Background").ignoresSafeArea())
}

