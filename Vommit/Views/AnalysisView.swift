import SwiftUI

struct AnalysisView: View {
    
    var body: some View {
        Text("Text")
    }
}

#Preview {
    AnalysisView()
        .preferredColorScheme(.dark)
        .background(Color("Background").ignoresSafeArea())
}
