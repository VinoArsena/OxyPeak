import SwiftUI

struct SetupManualView: View {
    
    var body: some View {
        Text("Text")
    }
}

#Preview {
    SetupManualView()
        .preferredColorScheme(.dark)
        .background(Color("Background").ignoresSafeArea())
}

